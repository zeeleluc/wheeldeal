<div>
    @if ($hasDraft)
        <div class="bg-yellow-100 border-b border-yellow-300 text-yellow-900 text-sm text-center py-2 px-4">
            {{ __('Please login or register to finalize your reservation') }}
        </div>
    @endif
</div>
